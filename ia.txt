Tabla:
Me ayudas a hacer esa tabla en el ANALISIS.md

me ayudo a rellenar los datos de la tabla


Etapa1:
Importa el arreglo en App.jsx y comprueba en la consola que se carga (console.log)

me fue util para mostrar los datos en la consola


Etapa2:
En MascotaCard.jsx, el componente recibe a través de props: nombre, raza, edad, especie, descripción, características,
para mostrar la información de manera visual y organizada, aplicando un estilo distinto según la especie
(un color/etiqueta diferente para perro, gato, otros).

me fue util para mostrarme los datos de las distintas mascotas 


Etapa3:
En ListaMascotas.jsx, recibe el array de mascotas a través de props, itera sobre el array utilizando .map() y renderiza un MascotaCard por cada mascota,
asignando adecuadamente la prop key usando el id de cada elemento (evita utilizar el índice del array).
Y en el App.jsx se renderiza ListaMascotas pasando los datos importados.

esta implementación me ayudó a entender cómo iterar un arreglo con .map() y renderizar un componente para cada ítem de la lista.


Etapa4:
En FiltroEspecie.jsx, incluye un selector para filtrar por la especie:
'Todas', 'Perro', 'Gato', 'Otro', y en App.jsx utiliza useState para almacenar el filtro seleccionado y un campo de búsqueda por nombre

esta implementación me ayudó a entender cómo usar useState para almacenar el filtro elegido y el texto de búsqueda,
haciendo que la aplicación actualice automáticamente los resultados.


Etapa5:
Destaca de manera visual los elementos con adopcionUrgente: true (como un contorno diferente), 
incorpora un contador que indique cuántas mascotas requieren adopción urgente y asegúrate de que toda la aplicación funcione sin errores ni advertencias en la consola.

esta implementación me ayudó a entender cómo aplicar estilos condicionales en React,
resaltando de manera visual las mascotas que requieren adopción inmediata.


modificacion:

se modifico el diseño de la pagina a uno mas sencillo
